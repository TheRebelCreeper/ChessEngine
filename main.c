#include <stdio.h>
#include <stdlib.h>
#include "bitboard.h"

int main()
{
	//U64 board = 0x0101010101010101ULL << 2;
	//set_square(board, e4);
	//clear_square(board, e4);
	print_bitboard(Rank8);
	return 0;
}